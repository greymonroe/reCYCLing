% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_repeat_barcode.R
\name{plot_repeat_barcode}
\alias{plot_repeat_barcode}
\title{Barcode visualisation of identical repeat units in a zoom window}
\usage{
plot_repeat_barcode(
  ps,
  zoom,
  palette = "Dark2",
  line_width = 2,
  base_size = 6,
  label_size = 2,
  show_labels = TRUE,
  gapsize = 0.9,
  vjusttext = -0.1
)
}
\arguments{
\item{ps}{Pairwise-identical \code{data.table} (such as an element of
\code{ps_results$ps_list} from \code{\link{run_sim_ps}}, or the output
of \code{\link{pairs_identical}}).}

\item{zoom}{Numeric vector of length 2 \code{c(min, max)} defining the
window of monomer positions to display.}

\item{palette}{Character. \pkg{RColorBrewer} palette name for colouring
identical-sequence groups. Default \code{"Dark2"}.}

\item{line_width}{Numeric. Segment line width. Default 2.}

\item{base_size}{Numeric. Base font size for \code{theme_void}. Default 6.}

\item{label_size}{Numeric. Size of position-number text labels. Default 2.}

\item{show_labels}{Logical. Whether to show numeric position labels.
Default \code{TRUE}.}

\item{gapsize}{Numeric. Fraction of the segment width to use as the gap
between segments. Default 0.9.}

\item{vjusttext}{Numeric. Vertical justification for position labels.
Default -0.1.}
}
\value{
A \code{ggplot2} object.
}
\description{
Draws a linear barcode in which each monomer position in the zoom window
is represented by a coloured segment. Monomers sharing the same sequence
receive the same colour, making it easy to see the pattern of identical
copies within a sub-region of the array.
}
\examples{
\dontrun{
sim <- run_sim_ps(n = 1, max_t = 500, mu_total = 3e-5)
ps  <- sim$ps_list[[1]]
plot_repeat_barcode(ps, zoom = c(1, 100))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ps_summary.R
\name{plot_ps_summary}
\alias{plot_ps_summary}
\title{Combined multi-panel summary plot}
\usage{
plot_ps_summary(sim, title = NULL, rel_heights = c(2, 1.5))
}
\arguments{
\item{sim}{Output from \code{\link{run_sim_ps}}.}

\item{title}{Character or \code{NULL}. Optional title placed above the
fingerprint panel. Default \code{NULL}.}

\item{rel_heights}{Numeric vector of length 2. Relative heights of the
fingerprint row and the histogram grid. Default \code{c(2, 1.5)}.}
}
\value{
A \pkg{cowplot} grid object (a \code{ggplot2} grob).
}
\description{
Assembles five diagnostic panels into a single figure using
\pkg{cowplot}: the repeat fingerprint on top (full width), and four
histogram panels in a 2x2 grid below.
}
\examples{
\dontrun{
sim <- run_sim_ps(max_t = 500, mu_total = 3e-5)
plot_ps_summary(sim, title = "mu = 3e-5")
}
}

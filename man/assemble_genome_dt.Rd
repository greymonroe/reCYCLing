% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_stats.R
\name{assemble_genome_dt}
\alias{assemble_genome_dt}
\title{Assemble an observed single-haplotype genome from per-chromosome fastas}
\usage{
assemble_genome_dt(
  files,
  chrom_field = 3L,
  num_field = 4L,
  dir_field = 7L,
  sep = "_"
)
}
\arguments{
\item{files}{Character vector of per-chromosome fasta paths.}

\item{chrom_field, num_field, dir_field}{1-based header fields (split on
\code{sep}) holding the chromosome id, monomer position, and strand.}

\item{sep}{Header field separator.}
}
\value{
data.table with columns \code{chrom}, \code{num}, \code{bponly},
  \code{dir}, \code{aligned}.
}
\description{
Reads per-chromosome monomer fastas for ONE taxon / ONE haplotype and
returns the standard genome data contract used by
\code{repeat_genome_stats()}: one row per monomer with integer chromosome
rank, within-chromosome position, gapless sequence, orientation, plus the
gapped sequence (column \code{aligned}) for divergence computation.
}

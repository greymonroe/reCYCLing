% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_array.R
\name{init_array}
\alias{init_array}
\title{Initialise a tandem repeat array}
\usage{
init_array(
  l = 178,
  k0 = 10,
  init_sequence_type = c("random", "given"),
  ancestor_seq = NULL,
  alphabet = c("A", "C", "G", "T")
)
}
\arguments{
\item{l}{Integer. Length in base pairs of one monomer unit. Default 178.}

\item{k0}{Integer. Number of initial copies (all identical). Default 10.}

\item{init_sequence_type}{Character. \code{"random"} (draw a new monomer
from \code{alphabet}) or \code{"given"} (use \code{ancestor_seq}).}

\item{ancestor_seq}{Character scalar. Required when
\code{init_sequence_type = "given"}; must have \code{nchar() == l}.}

\item{alphabet}{Character vector of allowed bases. Default
\code{c("A","C","G","T")}.}
}
\value{
A character vector of length \code{k0}, every element the same
  monomer sequence.
}
\description{
Creates the starting array of \code{k0} identical repeat units used to seed
a simulation. The ancestral monomer can be drawn randomly from an alphabet
or supplied directly.
}
\examples{
# Random ancestral monomer, 10 copies of 30 bp
arr <- init_array(l = 30, k0 = 10)
length(arr)   # 10
nchar(arr[1]) # 30

# Supply your own starting sequence
ancestor <- paste(rep("A", 30), collapse = "")
arr2 <- init_array(l = 30, k0 = 5,
                   init_sequence_type = "given",
                   ancestor_seq = ancestor)
}

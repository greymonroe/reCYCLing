% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_stats.R
\name{sample_pair_distances}
\alias{sample_pair_distances}
\title{Sampled distances between identical monomer pairs}
\usage{
sample_pair_distances(mono, max_per_group = 200L, max_total = 60000L)
}
\arguments{
\item{mono}{data.table/data.frame with columns \code{bponly}, \code{num},
\code{dir}.}

\item{max_per_group}{Maximum sampled pairs per identical-sequence group.}

\item{max_total}{Maximum total pairs returned.}
}
\value{
data.table with columns \code{dist} (positional distance) and
  \code{inverted} (opposite orientations).
}
\description{
Uniformly samples pairs of identical monomers within each identical-sequence
group (bounding the O(n^2) blow-up for huge groups) and returns their
positional distances. The same sampler is used for simulated and real
arrays so the resulting distance distributions are comparable.
}

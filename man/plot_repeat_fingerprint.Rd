% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_repeat_fingerprint.R
\name{plot_repeat_fingerprint}
\alias{plot_repeat_fingerprint}
\title{Dot-plot fingerprint of identical repeat-unit pairs}
\usage{
plot_repeat_fingerprint(
  sim,
  rotate = FALSE,
  zoom = NULL,
  ptsize = 0.01,
  gridalpha = 0,
  gbreaks = 20,
  ptcol = "black"
)
}
\arguments{
\item{sim}{Output from \code{\link{run_sim_ps}}.}

\item{rotate}{Logical. If \code{TRUE}, rotate the plot 45 degrees using grid
graphics (draws directly to the active device and returns \code{NULL}). If
\code{FALSE} (default), return a \code{ggplot2} object.}

\item{zoom}{Numeric vector of length 2 \code{c(min, max)} defining a
sub-region of the array to zoom into. \code{NULL} (default) shows the
full array.}

\item{ptsize}{Numeric. Point size. Default 0.01.}

\item{gridalpha}{Numeric. Alpha for the fine grid lines. Default 0 (hidden).}

\item{gbreaks}{Integer. Approximate number of major axis breaks. Default 20.}

\item{ptcol}{Character. Point colour. Default \code{"black"}.}
}
\value{
A \code{ggplot2} object when \code{rotate = FALSE}; draws to the
  active graphics device (invisibly \code{NULL}) when \code{rotate = TRUE}.
}
\description{
Draws a dot-matrix ("fingerprint") plot in which each dot represents a pair
of identical monomers, placed at their respective positions along the array
axes. This visualisation reveals the duplication history of the array:
diagonal streaks near the main diagonal indicate local (tandem) duplications,
while off-diagonal blocks indicate distal duplications.
}
\examples{
\dontrun{
sim <- run_sim_ps(max_t = 500, mu_total = 3e-5)
plot_repeat_fingerprint(sim)
plot_repeat_fingerprint(sim, zoom = c(0, 200))
}
}

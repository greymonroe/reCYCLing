% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entropy.R
\name{kb_row_window_entropy}
\alias{kb_row_window_entropy}
\title{Sliding-window entropy along a monomer array}
\usage{
kb_row_window_entropy(kb, N)
}
\arguments{
\item{kb}{A monomer \code{data.table} with a \code{bponly} column (such as
an element of \code{monomers_list} from \code{\link{run_sim_ps}}).}

\item{N}{Integer. Window size in rows.}
}
\value{
A \code{data.table} with one row per window position and columns
  from \code{\link{shannon_entropyplus}} plus \code{pos} (focal row index).
}
\description{
Applies \code{\link{shannon_entropyplus}} over a sliding window of
\code{N} consecutive rows of a monomer data.table, returning per-window
entropy statistics.
}
\examples{
\dontrun{
sim <- run_sim_ps(n = 1, max_t = 200, mu_total = 3e-5)
kb  <- sim$monomers_list[[1]]
win <- kb_row_window_entropy(kb, N = 50)
}
}

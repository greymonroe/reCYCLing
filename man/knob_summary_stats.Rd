% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_stats.R
\name{knob_summary_stats}
\alias{knob_summary_stats}
\title{Summary statistics for one repeat array (schema v2)}
\usage{
knob_summary_stats(
  mono,
  aligned = NULL,
  div_cutoff = DIV_CUTOFF,
  nfine = DIST_NFINE,
  max_per_group = 200L
)
}
\arguments{
\item{mono}{data.table with columns \code{bponly}, \code{num}, \code{dir}
(sim: from \code{run_sim_ps()$monomers}; real: parsed fasta).}

\item{aligned}{Optional gapped MSA sequences in the same order (real data).}

\item{div_cutoff}{Monomer divergence filter (substitutions/monomer).}

\item{nfine}{Number of fine distance bins.}

\item{max_per_group}{Pair-sampling cap per identical group.}
}
\value{
A named list ("stat object"); see \code{knob_predictors()} to
  flatten it to a numeric vector.
}
\description{
Computes the canonical array-level stat object: retained size, mean
divergence from consensus (the clock), identical-pair redundancy, and the
fine log-binned identical-pair distance histogram. Monomers more than
\code{div_cutoff} substitutions from the consensus are dropped and the
consensus recomputed (applied identically to sim and real data).
}
